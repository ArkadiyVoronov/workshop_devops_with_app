# Grafana: настройка и дашборды

## Доступ к Grafana

- URL: http://localhost:3000
- Логин: `admin`
- Пароль: `workshop`

Prometheus-датаcорс провизионится автоматически из `config/grafana/provisioning/datasources/datasources.yml`.
Он указывает на сервис `prometheus` из `docker-compose.yml`.
